function mat2csv(mat_name)
data = load(string(mat_name, '.mat')).CaseData;
writetimetable(data.ECG, string(mat_name, 'data.csv'))
afib = strcmp(data.anntype, '(AFIB');
normal = strcmp(data.anntype, '(N');
anntype = zeros(size(afib));
for i = 2:length(anntype)
    if afib(i) == 0 && normal(i) == 0
        if afib(i-1)
            anntype(i) = 
        elseif normal(i-1)
            anntype(i) = 
        end
    end
end
